- Finished Nov2024 version.
- The software is installed in Conda environment named “pype_env”, python version 3.10. Check './Python_Prep' for details. 
- Used a slightly new version of ETH32 API 2.04 rather than 2.01.
    - The new API has been uploaded to Git as dependencies.
    - IMPORTANT: you need to install it manually. the [install.sh](http://install.sh) won’t handle it.
        - IMPORTANT: Winfort ETH32 is an old library and it only supports 32-bit archetecture. If you have a modern 64-bit machine (if you don’t understand, you are very likely modern), follow these steps for installation:
            - Install DHCP server to configure IP address.
            - You need i386 archetechture enbaled: sudo dpkg --add-architecture i386.
            - You need the following 32-bit packages to let the software run:
                - sudo apt-get install libc6:i386.
                - sudo apt-get install libpthread-stubs0-dev:i386.
                -Run the setting program. Find the device using a broadcast ip: such as X.X.X.255. (192.168.1.255).
    - IMPORTANT: I don’t remember whether I installed libezV24. But it won’t hurt to have it…
- PATH and PYTHONPATH editing:
    - PYPEDIR = ‘/usr/local/pype-python3/’
    - $PYPEDIR/lib added to $PATH
    - $PYPEDIR/lib, $PYPEDIR/bin, and $PYPEDIR/../p2m/ added to $PYTHONPATH.
- Working directory: 64bit_PYPE_Nov2024_WorkingCopy_FLAGGED. The zip was changed to ‘.tar.xz’ to support modern Ubuntu. ./build was changed accordingly to support .tar.xz, rather than .tar.gz.
- ‘./build_install_pype_python3.sh’ was edited. Now it’s updated in the Github repo (notes in here not important).
    - Non-important notes: change ‘sudo’ and ‘conda’ command to use the ‘conda’ environment properly. Commented a lot of stuff to prepare the environment. Commented a lot of stuff to prepare the environment.
    - IMPORTANT: Somba environment part was skipped. Even though it’s still in the .sh file.
    - IMPORTANT: The packages were installed manually.
- ./package/build.sh file:
    - Non important notes: All variable has been changed significantly so that they use the python under current environment, rather than the hard-coded /usr/lcoal/python one.
    - Change in source: Included ‘#include <unistd.h>’ in psems.c so that it can find getpid(). Don’t understand why it was not there. Multiple debugging in dacq files. Such as adding a retrun when the function psems_info() is called successfully.
- Config file and rig parameter file:
    - They need to be named properly as the machine name to be found correctly.
    - Suppose the PC’s name is eclat, put “Config-eclat” under .pyperc/. Put “rig-eclat.par” under ./pyperc/_none.
- At this point, the pype software should be able to work after the installation using Nov2024 package on a modern machine.

-Chenghang 11/2024